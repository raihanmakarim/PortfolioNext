import style from "./style.module.scss";

const Navbar = () => {
  //style
  const { background, container } = style;
  //usestate

  //function

  return (
    <div className={background}>
      <div className={container}>navabr</div>
    </div>
  );
};

export default Navbar;
